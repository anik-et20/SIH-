<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions required for WeatherGPT -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_dialog_info"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/ic_dialog_info"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherGPT"
        android:usesCleartextTraffic="true"> <!-- Allow HTTP traffic to backend on local network -->

        <!-- Splash Screen Launcher -->
        <activity
            android:name="com.example.weathergpt.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.WeatherGPT.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.action.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding Screen -->
        <activity
            android:name="com.example.weathergpt.OnboardingActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Main Dashboard Screen -->
        <activity
            android:name="com.example.weathergpt.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait" />

    </application>

</manifest>
